# Authority (host[:port]) of the device-database API.
# Leave unset to fall back to bundled mock data for local development.
API_AUTHORITY=xxx

# Keep this (preview) deployment out of search engines. Defaults to true.
# Set to false ONLY for a real production deploy that should be indexed.
NOINDEX=true
